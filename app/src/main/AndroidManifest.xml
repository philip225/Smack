<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kfa.training.smack">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
     Deviation from course, we have to allow a security override for API 28+, see
     res/xml/security_clear_text.xml for details.
     tools:targetApi="n" is set so the lint does not complain about android:networkSecurityConfig
     which targets API 24.
     See https://developer.android.com/studio/write/tool-attributes#toolstargetapi
     Application backup, this is set off since the data is stored externally.
     The only thing that could be backed up is the login token, stored in preferences, but it could
     be argued that this is a security issue.
     -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/security_clear_text"
        android:name=".Controller.App"
        tools:targetApi="n">
        <activity
            android:name=".Controller.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>